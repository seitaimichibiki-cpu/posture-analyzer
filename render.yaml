services:
  - type: web
    name: posture-analyzer
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT --workers 1 --worker-class gthread --threads 1 --timeout 300
    envVars:
      - key: PORT
        value: 10000
